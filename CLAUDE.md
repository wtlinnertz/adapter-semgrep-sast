# AIEOS Adapter — security.sast via semgrep

Claims: `security.sast` at 1.0.0. Contract: `aieos-governance-foundation/contracts/security.sast.contract.yaml`.
Findings schema: `findings/schemas/sarif-2.1.0.schema.json`.

Read the M4 section of `~/second-brain/AIEOS Spec-Driven CI-CD Implementation Plan.md`.

Python conventions: type hints, ruff, pytest. Unit tests mock subprocess.
